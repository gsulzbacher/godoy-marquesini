---
const { kicker, titulo, lead, center = false } = Astro.props;
---
<div class={`sec-head rv${center ? ' center' : ''}`}>
  {kicker && <span class={`kicker${center ? ' center' : ''}`}>{kicker}</span>}
  <h2>{titulo}</h2>
  {lead && <p class="lead">{lead}</p>}
</div>
